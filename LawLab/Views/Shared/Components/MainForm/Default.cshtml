@model string
<section id="form">
    <div class="container text-center wow fadeInDown">
        <div class="row">
            <h2>Самый простой способ стать частью Юридической Лаборатории</h2>
            <p class="large align-content-center">На регистрацию Вам потребуется не более 3-х минут. На успешную карьеру Вам потребуется вся жизнь!</p>
        </div>
        <div class="row">
            <div class="col-md-12 col-md-offset-1">
                <div class="align-content-center">
                    @if (((bool)User?.Identity?.IsAuthenticated))
                    {
                        <p>Мы рады снова Вас видеть на нашем сайте, @Model!</p>
                    }
                    else
                    {
                        <a asp-action="ChoiceRole" asp-controller="Form" class="btn btn-primary">Зарегистрироваться <i class="fa fa-chevron-circle-right"></i></a>
                    }
                </div>
            </div>
        </div>
    </div>
</section>